<div class="navbar-wrapper">
    <ul>
        <li class="navbar-links"><a href="/">Home</a></li>
        <li class="navbar-links"><a href="/projects">Projects</a></li>
        <li class="navbar-links"><a href="/about">About</a></li>
        <li class="navbar-links"><a href="/news">Blog</a></li>
    </ul>
</div>

<style>
    .navbar-wrapper {
        display: flex;
        justify-content: space-evenly;
        padding: 0;
        max-width: 100vw;
        min-width: 300px;
    }
    ul {
        max-width: 500px;
        min-width: 300px;
        display: flex;
        justify-content: space-evenly;
    }
    .navbar-links {
        width: 80px;
        height: 30px;
        margin: 0 10px;
        padding: 8px 0px 0px 0px;
        text-align: center;
        list-style-type: none;
    }
    a {
        /* width: 80px; */
        /* height: 40px; */
        text-decoration: none;
        color: #000;
    }
    a:is(:hover, :focus-within) {
		color: rgb(var(--accent));
	}

    .navbar-links {
        background-color: white;
		background-image: var(--accent-gradient);
		background-size: 400%;
		border-radius: 0.5rem;
		background-position: 100%;
		transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
    }

    .navbar-links:is(:hover, :focus-within) {
		background-position: 0;
	}
	.navbar-links:is(:hover, :focus-within) {
		background-color: rgb(var(--accent));
        color: #fff;
	}
    .navbar-links:is(:hover, :focus-within) a {
        color: #fff;
	}
</style>